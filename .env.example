# API URL — leave blank to use Vite proxy (/api) in development
VITE_API_URL=

# AWS Cognito — leave blank to bypass auth in development
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=

# Set to "true" to enable MSW mock API (auto-enabled when no VITE_API_URL is set)
VITE_ENABLE_MOCKS=true
